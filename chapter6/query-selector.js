console.log('Query Selector');

const para = document.querySelector('body > h1');
console.log('Query Selector',para);

const paras = document.querySelectorAll('p');
console.log('Query Selector All',paras);