Single quotes can interfere on string's closing

AND -> &&
OR -> ||
NOT -> !

continue; -> Next iteration on loop
break; -> Exits loop

Switch uses === comparations

You can declare variables with same name, if one has block scope